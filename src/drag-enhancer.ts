// Enhances the existing game controller without replacing its game logic.
// The original pointerdown/up handlers still perform the real swap and match
// resolution; this layer owns the visual drag and neighbour displacement.
const install=()=>{const board=document.querySelector<HTMLElement>("#board") as (HTMLElement&{onpointerdown:any;onpointermove:any;onpointerup:any;onpointercancel:any;onlostpointercapture:any})|null;if(!board)return false;const original={down:board.onpointerdown,up:board.onpointerup,cancel:board.onpointercancel,lost:board.onlostpointercapture};let drag:{x:number;y:number;i:number;target:number;axis:"x"|"y";dir:number;selected:HTMLElement;neighbor:HTMLElement|null;step:number;pending:boolean}|null=null;const cleanup=()=>{board.classList.remove("tracking");board.querySelectorAll<HTMLElement>(".dragging,.drag-target,.touching").forEach(el=>{el.classList.remove("dragging","drag-target","touching");el.style.transform="";el.style.transition=""})};board.onpointerdown=(e:PointerEvent)=>{original.down?.(e);const tile=(e.target as HTMLElement).closest<HTMLElement>(".tile");if(!tile)return;const i=Number(tile.dataset.i);drag={x:e.clientX,y:e.clientY,i,target:-1,axis:"x",dir:0,selected:tile,neighbor:null,step:board.clientWidth/8,pending:false};tile.classList.add("touching")};board.onpointermove=(e:PointerEvent)=>{if(!drag||drag.pending)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(Math.hypot(dx,dy)<5)return;if(drag.dir===0){drag.axis=Math.abs(dx)>=Math.abs(dy)?"x":"y";drag.dir=(drag.axis==="x"?dx:dy)>=0?1:-1;drag.target=drag.i+(drag.axis==="x"?drag.dir:drag.dir*8);const valid=drag.target>=0&&drag.target<64&&!(drag.axis==="x"&&((drag.dir>0&&drag.i%8===7)||(drag.dir<0&&drag.i%8===0)));if(!valid){drag.dir=0;return}drag.neighbor=board.querySelector<HTMLElement>(`.tile[data-i="${drag.target}"]`);drag.selected.classList.add("dragging");drag.neighbor?.classList.add("drag-target");board.classList.add("tracking")};if(drag.dir===0)return;const delta=Math.max(-drag.step,Math.min(drag.step,drag.axis==="x"?dx:dy));const axis=drag.axis;const d=drag.dir;drag.selected.style.transition="none";drag.selected.style.transform=axis==="x"?`translate3d(${delta}px,0,0) scale(.96)`: `translate3d(0,${delta}px,0) scale(.96)`;if(drag.neighbor){drag.neighbor.style.transition="none";const other=delta-d*drag.step;drag.neighbor.style.transform=axis==="x"?`translate3d(${other}px,0,0) scale(.98)`: `translate3d(0,${other}px,0) scale(.98)`}};board.onpointerup=(e:PointerEvent)=>{if(!drag)return;const d=drag;const delta=d.axis==="x"?e.clientX-d.x:e.clientY-d.y;const threshold=Math.max(20,d.step*.30);const commit=d.dir!==0&&Math.abs(delta)>=threshold;const originalUp=original.up;d.pending=commit;if(!commit){cleanup();drag=null;originalUp?.(e);return}const full=d.dir*d.step;d.selected.style.transition="transform .18s cubic-bezier(.2,.9,.2,1)";d.selected.style.transform=d.axis==="x"?`translate3d(${full}px,0,0) scale(.98)`: `translate3d(0,${full}px,0) scale(.98)`;if(d.neighbor){d.neighbor.style.transition="transform .18s cubic-bezier(.2,.9,.2,1)";d.neighbor.style.transform=d.axis==="x"?`translate3d(${-full}px,0,0) scale(.98)`: `translate3d(0,${-full}px,0) scale(.98)`}setTimeout(()=>{if(drag===d){originalUp?.(e);drag=null}},190)};board.onpointercancel=(e:PointerEvent)=>{if(drag){cleanup();drag=null}original.cancel?.(e)};board.onlostpointercapture=(e:Event)=>{if(drag?.pending)return;drag=null;cleanup();original.lost?.(e)};return true};let tries=0;const timer=setInterval(()=>{if(install()||++tries>100)clearInterval(timer)},50)};install();